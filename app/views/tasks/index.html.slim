h1 =t('.title')
= link_to t('.new'), new_task_path
- @tasks.each do |task|
  div
    = link_to task.title, task_path(task)
    = task.description
    = link_to t('.edit'), edit_task_path(task)
    = link_to t('.delete'), task_path(task),method: :delete, data: {confirm: '削除しますか？'}
